# Viewtron-API

## Installation
### 1) Clone Repository
### 2) Install Flask
Run the following commands in repository destination
```
 python3 -m venv .venv
 . .venv /bin/activate
 pip install Flask
```
### 3) Run Script
```
flask --app Viewtron-XML-Parser run --host=0.0.0.0
```
